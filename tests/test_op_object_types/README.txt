'op' returns several object types other than item types. These include things like:
- groups
- users
- vaults
and possibly others. Those object types should be tested here.

Tests calling any methods on the OP class, or requiring a call to the 'op' CLI executable should not go here.
